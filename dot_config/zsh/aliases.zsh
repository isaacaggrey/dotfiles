alias ghi='ghi --no-pager'
#alias git='hub' # TODO: figure out how to install hub via nixos package
alias grep='grep --color'
alias gw='./gradlew --console=rich'
alias l='ls'
alias la='ls -a'
alias ll='ls -l'
alias ls='ls --color'
alias o='fasd -a -e xdg-open' # quick opening files with xdg-open
alias pacman='pacman --color always'
alias rm='rm -I' # safer rm
alias ssh='ssh -C'
alias sudo='sudo '
alias v='fasd -e nvim' # quick opening files with vim
alias addon-sdk="cd /opt/addon-sdk-git && source bin/activate; cd -"

alias -s txt='vim'
alias -s pdf='xdg-open'
alias -s odt='xdg-open'
